const mocha = require('mocha')
const describe = mocha.describe
const it = mocha.it
const assert = require('assert')

describe('another-todo-api', () => {
  it('should has tests', () => {
    assert.ok('Everything is fine')
  })
})
